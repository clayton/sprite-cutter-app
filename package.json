{
  "name": "sprite-cutter-app",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=sprite-cutter-app --branch=master",
    "deploy:dry-run": "npm run build"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
